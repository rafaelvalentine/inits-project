import React from 'react'

const index = ({first}) => {
  // return (
  //   <div id='page-numbers'>
  //     <span> Showing {first} - {pageUsers} of {allUsers || ''}  </span>
  //     <div>
  //       {pages.currentPage !== 1 ? <Button onClick={handlePagnationDown} value='Prev'>Prev <img className='double_arrow rotate ' src={require('../../assets/images/double_arrow-left.svg')} alt='double_arrow' /> </Button> : null}
  //       <span> {pages.currentPage} / {Math.ceil(users.length / pages.usersPerPage)} </span>
  //       {pages.currentPage >= Math.ceil(users.length / usersPerPage) ? null : <Button onClick={handlePagnationUp} type='button' value='Next'>Next <img className='double_arrow' src={require('../../assets/images/double_arrow-right.svg')} alt='double_arrow' /></Button> }
  //     </div>
  //   </div>
  // )
}

export default index
